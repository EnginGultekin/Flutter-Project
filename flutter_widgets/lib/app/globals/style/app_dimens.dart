import 'package:flutter/material.dart';

class AppDimens {
  static const Radius smallRadius = Radius.circular(10);
  static const Radius mediumRadius = Radius.circular(20);
  static const Radius largeRadius = Radius.circular(30);

  static final BorderRadius xSmallBorderRadius = BorderRadius.circular(5);
  static final BorderRadius smallBorderRadius = BorderRadius.circular(10);
  static final BorderRadius mediumBorderRadius = BorderRadius.circular(20);
  static final BorderRadius largeBorderRadius = BorderRadius.circular(30);

  static const double mediumDeviceWidth = 1250;
  static const double smallDeviceWidth = 768;
  static const int xsPadding = 10;
  static const int smPadding = 20;
  static const int mdPadding = 30;
  static const int lgPadding = 40;
  static const int xlPadding = 50;
}
